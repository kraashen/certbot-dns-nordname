"""Compatibility shim for legacy Python packaging tools.

The canonical build configuration for this project is in pyproject.toml
with Hatchling as the build backend.
"""

from setuptools import setup


setup()
